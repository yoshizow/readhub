#!/bin/sh

ref="$1"
rev="$3"

set -e

if [ "$ref" != "refs/heads/master" ] ; then
   exit 0
fi

echo "readhub-update-hook: args: $@"

curl -fsSL http://localhost:4000/repos/$GL_REPO/$rev/new

/usr/local/bin/checkout-and-index-wrapper $GL_REPO_BASE $GL_REPO $rev

exit 0
